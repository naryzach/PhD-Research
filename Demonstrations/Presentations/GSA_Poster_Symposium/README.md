# GSA Graduate Poster Symposium (October 27, 2026)

Build: `python make_poster_figures.py`, `python make_pipeline_figure.py`, then `python build_poster.py`.
Text lives in `poster_data.yaml`. Size is assumed to be 48 x 36 in; see `poster_plan.md` for open items.
The pocket-analysis and pipeline structure panels are in `figures/src/`.
