# Core
# NOTE: plain `pip install torch` grabs the newest CUDA build, which may be too
# new for your GPU driver (symptom: "NVIDIA driver too old", falls back to CPU).
# On a cluster, match the build to the driver's CUDA (see `nvidia-smi`), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.1
transformers>=4.40
einops             # required by the ESM++/ESM-C remote modeling code
numpy
pandas
pyarrow            # parquet I/O for the prepared splits
scikit-learn
pyyaml
tqdm

# Visualization (visualize.py)
matplotlib
seaborn
umap-learn         # UMAP dimensionality reduction; visualize.py falls back to PCA if absent
                   # (pulls in numba + llvmlite; see README for the recommended Python env)

# Interpretability (shap_hotspots.py)
shap
